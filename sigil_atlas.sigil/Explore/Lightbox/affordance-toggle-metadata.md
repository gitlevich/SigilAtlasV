show or hide what the @image knows about itself, overlaid on the @Lightbox.

Cmd-I toggles the overlay. Contents are whatever the @image carries — EXIF, capture time, dimensions, @source path; later, geolocation and detected faces when those exist. Toggle is a property of the @Lightbox, not of a particular @image; the state persists as I #walk.
