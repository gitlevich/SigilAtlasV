Exactly one pane is the driver at any moment. There is only one @attention, mine, and attention is undivided along a direction at a given instant — I cannot simultaneously be rearranging @Program and scrubbing @Timeline. The active pane is always visibly marked so the user knows which pane will receive the next input. Driver changes are atomic: there is no intermediate state in which two panes are both accepting input.
