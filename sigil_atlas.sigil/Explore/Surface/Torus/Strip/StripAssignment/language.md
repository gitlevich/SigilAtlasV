---
status: idea
---

# Strip Assignment

How @images from a @slice get placed into @strips on the @torus.

In @timelike mode: each @story becomes a strip. @Time flows left-to-right. Stories stack vertically — older above, newer below. All strips scrub in sync.

@spacelike mode uses @neighborhoods, not strips.

!deterministic.
