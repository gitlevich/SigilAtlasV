Remove a @thing from the library permanently, guarded by a confirmation (typically a small question-mark indicator on the × that requires a second click or explicit "yes"). Deletion cannot be undone — the @thing must be re-typed to return. This is deliberate: the × on a library pill is a rarer, more serious action than the × on an @AttractorControl pill, which merely #deactivates.
