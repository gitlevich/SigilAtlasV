---
status: idea
---

# Point Of View

The camera. Three values: x, y (position on @torus surface, wraps) and z (distance from surface).

At z=0: one @image fills the @frame. At z=max: entire @torus visible at micro-thumbnail resolution. Pan changes x, y. Zoom changes z. All continuous.

!fixed-fov.
