---
status: idea
---

# Embedding

Produced by a @VisionModel from an @Image. Same @VisionModel is !stable for a given @image: given the same one, it always embeds it in the same place in @AttentionLanguage@ContrastSpace.