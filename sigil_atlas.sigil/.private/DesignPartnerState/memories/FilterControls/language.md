@Slice is one of the product terms in the rewritten core language after pruning, replacing older self-referential framing around @Sigil. It denotes a bounded product view, but its ordering is not fully intrinsic: @RelevanceFilter implies that ordering can shift with @Mode and layout context, putting @Slice in concrete tension with @FilterControls whenever contextual ordering affects how a slice is presented.