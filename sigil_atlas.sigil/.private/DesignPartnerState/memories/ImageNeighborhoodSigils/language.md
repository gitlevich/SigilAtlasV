ImageNeighborhoodSigils are produced by aggregating image sigils after packing images into sigils that captured every contrast for every image, then recursively re-forming the corpus into multiple containment structures by gradually removing contrasts one by one rather than merely embedding and viewing it; they thus end up as multiple essentially top-level sigils in differing containment structures, and these hierarchies are the structures being sliced and traversed during @Explore. A slice in @Explore must be defined by how it intersects an ImageNeighborhoodSigil hierarchy, and parent regions in that hierarchy split into child regions when one contrast is reintroduced during @Explore. The underdefined center of this design should be clarified, in the user’s own language, by stating what appears in the fixed frame for each mode and what one arrow-key step means within that frame during @Explore. This ontology has been restored by the user and should be preserved without pruning in its current form; the assistant should work around its intentional structure rather than trimming it again unless explicitly asked by @UserPreferenceNoTrimming.