Slice is what happens when you apply a relevance filter on the corpus, and the user thinks this is quite clearly stated. In @Workspace/Corpus/Slice, an invariant says that when not otherwise ordered, the slice is ordered by the @Contrast specified as #time-direction—i.e. by @TimeDirection—and an affordance says time-direction allows definition of the @Contrast to treat as direction of time. This makes Slice appear intrinsically ordered by @TimeDirection in @Workspace/Corpus/Slice, creating tension with @Explore and @FilterControls, where ordering is described as depending on mode and layout (via @Timelike / @RelevanceFilter) rather than being intrinsic. In @Explore, the ambiguity under discussion is therefore not between Slice and mode in their domain-language definitions, but in whether the extra semantics attached under @Timelike, this concept, and @TimeDirection make time direction a property of Slice itself or only a timelike layout control.