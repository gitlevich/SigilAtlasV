This concept is a child sigil under @Explore for neighborhood semantics. It should capture the core semantic unit that code must implement consistently under @Explore. It should describe in plain product language what state exists, what operations transform it, and what invariants must survive those transformations. Anything that changes meaning across the whole app, such as how a neighborhood is computed, should live higher in @Explore rather than inside UI-specific sigils.