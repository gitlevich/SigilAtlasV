This concept is a child sigil under @Explore for view-state projection. It should capture the core semantic unit that code must implement consistently under @Explore. It should describe in plain product language what state exists, what operations transform it, and what invariants must survive those transformations. UI-specific sigils should describe projection and local interaction rules, while app-wide meaning should remain in higher-level semantics under @Explore.