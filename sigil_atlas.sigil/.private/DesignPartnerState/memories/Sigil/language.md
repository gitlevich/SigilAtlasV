Within @ImageNeighborhoodSigils, this concept now reads mostly as leftover ontology rather than core product model language: its app-local subtree under @Explore was deleted when the main leftover ontology was pruned from the spec, and Sigil is better understood as belonging to a reflective ontology layer intentionally distinct from the app layer under @Explore. The rewritten core framing now uses product terms like @FilterControls and @RelevanceFilter instead of the older self-referential sigil language. It still carries associations to fixed points and globally unique names in that leftover framework, and remains entangled with overlapping product language such as @RelevanceFilter, sigilControls, contrastspace, time direction, and sigil builder, without yet settling into one clean mental model around @ImageNeighborhoodSigils. A deleted subtree for this concept is known to exist and the current request is to restore it, but that content cannot be faithfully recovered from memory alone; proper restoration would require a source of truth such as project root or @git history, while a fallback is to reconstruct the subtree from remaining references and original intent.